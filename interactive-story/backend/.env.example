# OpenAI API Key for text generation (required)
OPENAI_API_KEY=<token>

# SiliconFlow API Key for image generation and TTS (required)
SILICONFLOW_API_KEY=<token>

# DeepSeek API Key for alternative text generation (optional)
DEEPSEEK_API_KEY=<token>

# Ark API Key for alternative text generation (optional)
ARK_API_KEY=<token>
